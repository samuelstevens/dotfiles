#!/usr/bin/env bash

if [[ -x "$HOME/.local/bin/gemini-cli" ]]; then
    exec "$HOME/.local/bin/gemini-cli" "$@"
elif command -v bunx >/dev/null 2>&1; then
    exec bunx --package @google/gemini-cli gemini "$@"
else
    exec npx -y @google/gemini-cli@latest "$@"
fi
